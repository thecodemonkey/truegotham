spring:
  datasource:
    url: jdbc:h2:file:/app/data/demo-db;DB_CLOSE_DELAY=-1;AUTO_SERVER=TRUE
  jpa:
    hibernate:
      ddl-auto: update
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
app:
  map:
    caching: false
